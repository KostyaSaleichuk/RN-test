import {StyleSheet} from 'react-native';

export const buttonStyles = StyleSheet.create({
  buttonContainer: {
    height: 45,
    borderRadius: 15,
    width: '80%',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
