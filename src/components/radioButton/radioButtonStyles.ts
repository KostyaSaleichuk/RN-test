import {StyleSheet} from 'react-native';

export const radioButtonStyles = StyleSheet.create({
  container: {
    height: 24,
    width: 24,
    borderRadius: 12,
    borderWidth: 2,
  },
  checked: {
    height: 24,
    width: 24,
    borderRadius: 12,
  },
});
