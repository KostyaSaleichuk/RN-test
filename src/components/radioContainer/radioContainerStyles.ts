import {StyleSheet} from 'react-native';

export const RadioContainerStyles = StyleSheet.create({
  text: {
    fontSize: 14,
    textAlign: 'center',
  },
  radioContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    width: '55%',
    height: '15%',
  },
});
